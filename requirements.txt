numpy
pandas
matplotlib
seaborn
h5py
hdf5plugin
mplhep
atlas_mpl_style
scikit-learn
torch
wandb
pytest

# TensorFlow:
# - Apple Silicon (M1/M2/M3): use Apple's build + Metal plugin
tensorflow-macos ; platform_system == "Darwin" and platform_machine == "arm64"
tensorflow-metal ; platform_system == "Darwin" and platform_machine == "arm64"

# - Linux / Windows / Intel macOS: regular tensorflow
tensorflow ; platform_system != "Darwin" or platform_machine == "x86_64"